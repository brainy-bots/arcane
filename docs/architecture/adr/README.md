# Architecture Decision Records (ADR)

Use this folder for **short, dated decisions** that implementers need on hand (e.g. “Unreal-native dedicated server for Chaos v1 demo,” UE version pin, tick sub-step policy).

**Suggested name:** `001-unreal-physics-integration-shape.md` (increment the prefix per new ADR).

Link new ADRs from [physics-backends-and-unreal.md](../physics-backends-and-unreal.md) where they constrain that integration path.
