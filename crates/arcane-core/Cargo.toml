[package]
name = "arcane-core"
version = "0.4.0"
edition = "2021"
description = "Arcane Engine — core traits and shared types (IF-01–04)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
